#ifndef M_ACTIVE
#define M_ACTIVE

namespace SDLEngine
{
    //Represents if the entity is active (if it should rendered, checked for collisions, ...) or not
    struct Active{};
}

#endif // !M_ACTIVE
